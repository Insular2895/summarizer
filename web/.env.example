# Optional when the API is served from another origin in production.
VITE_API_BASE_URL=

# Used only by Vite development; production identity comes from Cloudflare Access.
VITE_LOCAL_USER=local@example.test
